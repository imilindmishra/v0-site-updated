# sharp is unused: next.config sets images.unoptimized and the site is a static export.
# unrs-resolver ships prebuilt binaries, so its build script is not needed either.
allowBuilds:
  sharp: false
  unrs-resolver: false
verifyDepsBeforeRun: false
